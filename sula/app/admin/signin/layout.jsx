
export const metadata = {
    title: "Signin | Admin Masala",
  };

const SignInLayout = ({children}) => {
return (
        <div>
        {children}
        </div>
    );
};

export default SignInLayout;