export const metadata = {
    title: "Product Option | Admin Masala",
  };

const ProductCustomizationLayout = ({children}) => {
return (
        <section>
        {children}
        </section>
    );
};

export default ProductCustomizationLayout;