export const metadata = {
    title: "Product Customization | Admin Masala",
  };

const ProductOptionLayout = ({children}) => {
return (
        <section>
        {children}
        </section>
    );
};

export default ProductOptionLayout;