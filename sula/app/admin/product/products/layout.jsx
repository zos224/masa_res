export const metadata = {
    title: "Products | Admin Masala",
  };

  const ProductsLayout = ({children}) => {
    return (
      <section>
        {children}
      </section>
    );
  };

  export default ProductsLayout;