export const metadata = {
    title: "Update Order",
}
const UpdateOrder = ({children}) => {
    return (
        <section>
            {children}
        </section>
    )
}

export default UpdateOrder